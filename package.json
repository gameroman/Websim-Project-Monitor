{
  "name": "websim-project-monitor",
  "private": true,
  "type": "module",
  "imports": {
    "#config": "./config.json"
  },
  "scripts": {
    "format": "oxfmt --list-different && echo 'All files are already formatted' || oxfmt",
    "start": "bun --bun ./src/index"
  },
  "dependencies": {
    "websim": "^0.0.13"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "oxfmt": "^0.33.0"
  }
}
